server:
  port: ${PORT:8080}

spring:
  profiles:
    active: h2 # デフォルトは H2（ポートフォリオ用）。環境変数 SPRING_PROFILES_ACTIVE で上書き可能。
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

# 共通のログレベルなどをここに追加可能
